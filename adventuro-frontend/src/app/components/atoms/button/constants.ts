import {tuple} from "../../../utils";

export const DEFAULT = "default";
export const CONTAINED = "contained";
export const OUTLINED = "outlined";

export const Types = tuple(DEFAULT, CONTAINED, OUTLINED);
